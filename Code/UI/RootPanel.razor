@inherits PanelComponent
@namespace SBRacer.Ui

<root>
	<title class="speed">@( $"{CarController.Local.DisplaySpeed}MPH" )</title>
</root>

@code
{
	protected override int BuildHash()
	{
		return HashCode.Combine( CarController.Local.DisplaySpeed );
	}
}
